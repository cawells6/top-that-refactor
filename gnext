#!/bin/bash
# Root-level gnext wrapper for Bash shells
# Usage: gnext "commit message" branch-name

COMMIT_MSG="$1"
BRANCH_NAME="$2"

if [ -z "$BRANCH_NAME" ]; then
  echo "Usage: gnext \"commit message\" branch-name"
  exit 1
fi

./scripts/gnext.sh "$COMMIT_MSG" "$BRANCH_NAME"
